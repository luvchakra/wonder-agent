import { NotYetAvailable } from "@/modules/ui";

// Owned by Foundation Agent (Users/Roles/SSO/Tenant Settings/Audit Logs UI
// deferred per its own audit log) — the nav item renders per
// EXPERIENCE-P0-01.1, this placeholder avoids a broken link.
export default function SettingsPage() {
  return <NotYetAvailable feature="Administration" />;
}
