import { NotYetAvailable } from "@/modules/ui";

// Owned by Operations Agent (Module 10), not yet dispatched — the nav item
// renders per EXPERIENCE-P0-01.1, this placeholder avoids a broken link.
export default function ReportsPage() {
  return <NotYetAvailable feature="Reports" />;
}
